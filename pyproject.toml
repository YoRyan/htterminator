[project]
name = "htterminator"
version = "0.1.0"
description = "Make web scrapers appear more human-like by proxying their requests through a real browser session."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "camoufox~=0.5",
]

[dependency-groups]
dev = [
    "mypy~=2.3",
    "pytest~=9.1",
    "ruff~=0.16",
]
